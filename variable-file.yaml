- name: variable file
  hosts: local
  connection: local
  vars_file:
    course.yaml

  tasks:
    - name: course info
      ansible.builtin.debug:
        msg: "hello i'm learning {{ course }}, duration of {{ duration }}, Trainer is: {{ trainer }}"